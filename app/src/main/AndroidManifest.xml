<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nz.pbomb.xposed.anzmods">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/MainTheme">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Allows users to bypass root detection on ANZ GoMoney New Zealand and any carrier variant of Semble to take advantage of Wallet features from either application from rooted or non-supported devices." />
        <meta-data
            android:name="xposedminversion"
            android:value="30" />

        <activity
            android:name="nz.pbomb.xposed.anzmods.MainActivity"
            android:label="@string/label_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="nz.pbomb.xposed.anzmods.HelpActivity"
            android:label="@string/label_help" />

        <activity
            android:name="nz.pbomb.xposed.anzmods.ContactActivity"
            android:label="@string/label_main_contact"/>

        <activity
            android:name="nz.pbomb.xposed.anzmods.AboutActivity"
            android:label="@string/label_about">
        </activity>

        <activity
            android:name="nz.pbomb.xposed.anzmods.PrefActivity"
            android:label="Potato">
        </activity>
    </application>


</manifest>
